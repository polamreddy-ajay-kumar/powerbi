const months=['Oct','Nov','Dec','Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep'];let revenueChart,channelChart;const $=s=>document.querySelector(s);const $$=s=>document.querySelectorAll(s);function toast(message){const el=$('#toast');el.textContent=message;el.classList.add('show');setTimeout(()=>el.classList.remove('show'),2800)}function initCharts(){const gridColor='#eef0f5';revenueChart=new Chart($('#revenueChart'),{type:'line',data:{labels:months,datasets:[{label:'Revenue',data:[62,72,68,81,75,91,86,102,96,111,106,128],borderColor:'#6d5dfc',backgroundColor:'rgba(109,93,252,.10)',fill:true,tension:.4,pointRadius:3,pointBackgroundColor:'#fff',pointBorderWidth:2,pointBorderColor:'#6d5dfc'}]},options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{display:false},tooltip:{backgroundColor:'#202a3a',padding:10,displayColors:false,callbacks:{label:c=>'$'+c.raw+'k'}}},scales:{x:{grid:{display:false},border:{display:false},ticks:{color:'#a4abba',font:{size:10}}},y:{grid:{color:gridColor},border:{display:false},ticks:{color:'#a4abba',font:{size:10},callback:v=>'$'+v+'k'},beginAtZero:true}}}});channelChart=new Chart($('#channelChart'),{type:'doughnut',data:{labels:['Direct','Organic search','Referral','Social media'],datasets:[{data:[42,28,18,12],backgroundColor:['#6d5dfc','#48b6e8','#f5af47','#67c69d'],borderWidth:0,hoverOffset:4}]},options:{cutout:'76%',responsive:true,maintainAspectRatio:false,plugins:{legend:{display:false},tooltip:{callbacks:{label:c=>c.label+': '+c.raw+'%'}}}}});renderLegend()}function renderLegend(){const colors=['#6d5dfc','#48b6e8','#f5af47','#67c69d'];const labels=['Direct','Organic search','Referral','Social media'];$('#channelLegend').innerHTML=labels.map((x,i)=>`<div class="legend-item"><i style="background:${colors[i]}"></i><span>${x}</span><b>${[42,28,18,12][i]}%</b></div>`).join('')}function showView(view){$$('.page-view').forEach(x=>x.classList.add('hidden'));$('#'+view+'View').classList.remove('hidden');$$('.nav-item[data-view]').forEach(x=>x.classList.toggle('active',x.dataset.view===view));$('#pageTitle').textContent=view[0].toUpperCase()+view.slice(1);if(view==='datasets')renderDataset()}function setupNav(){$$('.nav-item[data-view]').forEach(x=>x.addEventListener('click',()=>showView(x.dataset.view)))}function openModal(){$('#modal').classList.remove('hidden');$('#reportName').focus()}function closeModal(){$('#modal').classList.add('hidden')}function setupModal(){['newReport','newReport2','createReport'].forEach(id=>{const el=$('#'+id);if(el)el.addEventListener('click',openModal)});$('#modalClose').onclick=closeModal;$('#cancelModal').onclick=closeModal;$('#createModal').onclick=()=>{const name=$('#reportName').value.trim()||'Untitled report';localStorage.setItem('lastReport',name);closeModal();toast(`${name} created successfully`);$('#reportName').value=''}}function setupUpload(){['uploadButton','datasetUpload','emptyUpload'].forEach(id=>{const el=$('#'+id);if(el)el.addEventListener('click',()=>$('#fileInput').click())});$('#fileInput').addEventListener('change',e=>{const file=e.target.files[0];if(!file)return;const reader=new FileReader();reader.onload=event=>{try{const text=event.target.result;let rows;if(file.name.endsWith('.json'))rows=JSON.parse(text);else rows=parseCSV(text);if(!Array.isArray(rows)||!rows.length)throw Error('No rows');localStorage.setItem('dataset',JSON.stringify({name:file.name,rows}));renderDataset();showView('datasets');toast(`${file.name} imported successfully`)}catch(err){toast('Could not read this file. Please use CSV or JSON.')}};reader.readAsText(file)})}function parseCSV(text){const lines=text.trim().split(/\r?\n/);const headers=splitCSV(lines.shift());return lines.map(line=>{const vals=splitCSV(line);return headers.reduce((o,h,i)=>(o[h.trim()]=vals[i]?.trim()||'',o),{})})}function splitCSV(line){const out=[];let value='',quote=false;for(const char of line){if(char==='"')quote=!quote;else if(char===','&&!quote){out.push(value.replace(/^"|"$/g,''));value=''}else value+=char}out.push(value);return out}function renderDataset(){const saved=JSON.parse(localStorage.getItem('dataset')||'null');if(!saved){$('#datasetEmpty').classList.remove('hidden');$('#datasetTable').classList.add('hidden');return}const rows=saved.rows;const cols=Object.keys(rows[0]);$('#datasetEmpty').classList.add('hidden');$('#datasetTable').classList.remove('hidden');$('#datasetTable').innerHTML=`<div class="panel-header"><div><h2>${saved.name}</h2><p class="muted">${rows.length} rows · ${cols.length} columns</p></div><button class="secondary-button" id="removeDataset">Remove</button></div><div style="overflow:auto;margin-top:20px"><table style="width:100%;border-collapse:collapse;font-size:12px"><thead><tr>${cols.map(c=>`<th style="text-align:left;padding:11px;background:#f7f8fc;border-bottom:1px solid #e8ebf2">${c}</th>`).join('')}</tr></thead><tbody>${rows.slice(0,50).map(r=>`<tr>${cols.map(c=>`<td style="padding:11px;border-bottom:1px solid #f0f1f5">${r[c]??''}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`;$('#removeDataset').onclick=()=>{localStorage.removeItem('dataset');renderDataset();toast('Dataset removed')}}function setupMisc(){$('#addFavorite').onclick=()=>{$('#favorites').innerHTML='<div class="favorite-item" style="padding:9px 0;color:#6f798a">★ Executive Overview</div>';toast('Added to favorites')};$('#editDashboard').onclick=()=>toast('Dashboard editor is ready for customization');$$('.open-report').forEach(x=>x.onclick=()=>{showView('overview');toast('Report opened')});$('#periodSelect').onchange=e=>{toast(`Showing ${e.target.value.toLowerCase()}`)}}document.addEventListener('DOMContentLoaded',()=>{setupNav();setupModal();setupUpload();setupMisc();initCharts()});
